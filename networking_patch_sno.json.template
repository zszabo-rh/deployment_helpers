{
  "service_networks": [
    {
      "cidr": "fd02::/112",
      "cluster_id": "${CLUSTER_ID}"
    }
  ],
  "machine_networks": [
    {
      "cidr": "${HOST_NETWORK}",
      "cluster_id": "${CLUSTER_ID}"
    }
  ],
  "cluster_networks": [
    {
      "cidr": "fd01::/48",
      "cluster_id": "${CLUSTER_ID}",
      "host_prefix": 64
    }
  ]
}